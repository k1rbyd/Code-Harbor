#include<stdio.h>
#include<stdlib.h>

struct link{
    struct link *prv;
    int data;
    struct link *nxt;
};

int main(){
    struct link temp,front,head;
    head=(struct link *)malloc(sizeof(struct link));
    front=0;
    while(1){
        if 
    }
}