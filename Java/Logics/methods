method is basically just a fancy name for function

syntax,

    ==> returntype name()
        {

        }

example,
void say(int n)
{
    for(int i=0;i<n;i++)
    {
        System.out.println("hellloo thereee");
    }
}

void multitable(int n)
{
    for(int i=1;i<=10;i++)
    {
        System.out.printf("%d * %d = %d",n,i,i*n).println();
    }
}

==> to get the list of methods , we can just use

    /methods

==> to expand any method, we just have to type

    /list multitable

==> to edit any methods, we can just type

eg:
    /edit multitable()

==> if we want to save and load them, then we have to

    /save {some_name}.txt

==> we also have method overloading

and if we give /list method_name, it gives us all types

eg:

void say(int n)
{
    for(int i=0;i<n;i++)
    {
        System.out.println("hellloo thereee");
    }
}

void say()
{
    System.out.println("Nothing here");
}

/list say
